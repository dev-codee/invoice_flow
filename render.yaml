services:
  - type: web
    name: invoiceflow
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn invoiceflow.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0